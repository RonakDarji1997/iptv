<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rootLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    android:background="#FF000000">

    <!-- Main Side Navigation Container -->
    <FrameLayout
        android:id="@+id/sideNavContainer"
        android:layout_width="wrap_content"
        android:layout_height="match_parent" />

    <!-- Category Sidebar Container -->
    <FrameLayout
        android:id="@+id/categorySidebarContainer"
        android:layout_width="wrap_content"
        android:layout_height="match_parent" />

    <!-- Live TV Channels Container (fills remaining space) -->
    <FrameLayout
        android:id="@+id/liveTVContainer"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:visibility="gone" />

    <!-- VOD Container for Movies/Series (fills remaining space) -->
    <FrameLayout
        android:id="@+id/vodContainer"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:visibility="gone" />

    <!-- Search Container (fills remaining space) -->
    <FrameLayout
        android:id="@+id/searchContainer"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:visibility="gone" />

    <!-- Settings Container (fills remaining space) -->
    <FrameLayout
        android:id="@+id/settingsContainer"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:visibility="gone" />

    <!-- VOD Player Container (fullscreen overlay) -->
    <FrameLayout
        android:id="@+id/vodPlayerContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone" />

    <!-- Series Detail Container (fullscreen overlay) -->
    <FrameLayout
        android:id="@+id/seriesDetailContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone" />

</LinearLayout>
